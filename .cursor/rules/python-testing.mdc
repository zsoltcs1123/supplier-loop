---
globs: *.py
alwaysApply: false
---

# Testing Standards

## Test Organization

- **Mirror source structure** - Tests live in `tests/` under each package, mirroring the source structure
- **Test markers** - Categorize every test with a marker:
  - `@pytest.mark.unit` - Fast, no I/O
  - `@pytest.mark.integration` - Requires services or real I/O
  - `@pytest.mark.slow` - Long-running tests
  - `@pytest.mark.api` - API endpoint tests

## Style

- **No docstrings** on tests - Test names should be self-explanatory
- **No comments** - Clear test structure makes comments unnecessary
- **Descriptive names** - `test_<subject>_<expected_behavior>_when_<condition>`
- **Mock external dependencies** - Use `unittest.mock` or `pytest-mock`, never make real API/DB calls in unit tests

## What to Test (and What Not To)

- **Don't test language features** - Do not write tests that verify standard library or language behavior (dataclass defaults, dict construction, import availability, `callable()` checks). Only test logic, constraints, or integrations that _we_ defined. If removing the test wouldn't risk missing a regression in _our_ code, the test shouldn't exist.
- **Test behavior, not implementation** - Focus on what the code does, not how
- **Prioritize constraints and business rules** - Validation logic, edge cases, error handling
- **Use parametrize** to cover boundary values without duplicating test bodies

## Forbidden Patterns

- **No `monkeypatch.setattr` against first-party modules** - Patching attributes on your own project's modules (`monkeypatch.setattr(my_package.foo, "bar", fake)`) tests wiring, not behavior. It couples tests to import topology and breaks on innocuous refactors. Patching is only acceptable for stdlib boundaries (e.g. `time.sleep`) or external libraries you do not own.
- **No `importlib.import_module` as a patch anchor** - If a test uses `importlib.import_module("my_package.x")` to obtain a handle for later patching, delete it. Use direct `from my_package.x import thing` and either test the function purely, or use a real in-memory fixture (e.g. the shared `conn` fixture for SQLite).
- **Prefer real fakes over mocks** - For DB-backed logic, use a shared `conn` fixture (`sqlite3.connect(":memory:")` + migrations). Such tests should be marked `@pytest.mark.integration`, not `@pytest.mark.unit`. Pure functions stay in unit tests with no fixtures.
- **No tests of wiring/dispatch** - Do not write a test whose only assertion is "function A called collaborator B with arguments X". If the dispatch logic is worth testing, test it via its observable effect through the public API.
