# SL-V3 — Live development round reaches submit_results

**Status:** active

**Path:** SL

## Scenarios

- With `**SUPPLIER_SIM_MCP_URL**` and `**SUPPLIER_SIM_TOKEN**` set, call request_dev_round(), run the loop, inspect the simulator echo of submit_results. The payload has an entry for every relevant supplier. Extract may be crude. Vision is not required.

## Coverage

unclaimed

## Lifecycle

active
